PORT=3003
DB_PATH=./chicken-scratch.db
AUTH_THRESHOLD=50
NODE_ENV=development

# Encryption key for biometric data at rest (AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ENCRYPTION_KEY=
